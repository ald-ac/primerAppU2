<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.appsmoviles.u2primerapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.U2PrimerApp">
        <activity
            android:name=".ResultadoIvaActivity"
            android:label="Resultados IVA"
            android:exported="true" />
        <activity
            android:name=".ResultadoCirculoActivity"
            android:label="Resultados Circulo"
            android:exported="true" />
        <activity
            android:name=".IcmActivity"
            android:exported="true"
            android:label="IMC" />
        <activity
            android:name=".IvaActivity"
            android:exported="true"
            android:label="IVA" />
        <activity
            android:name=".CirculoActivity"
            android:exported="true"
            android:label="Circulo" />
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.U2PrimerApp.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>